<% if user.followed_by?(current_user) %>
  <%=link_to "フォロー中", user_friends_path(user.id), method: :delete, class: "user_friends", remote: true %>
<% else %>
  <%=link_to "フォローする", user_friends_path(user.id), method: :post, class: "user_friends", remote: true %>
<% end %>
